class dontsayNumber3 {
  static say(number) {
    if (number.toString().indexOf("3") > -1) return "pass";
    return number;
    //----------------------//
    // const str = number.toString;
    // for(let i = 0; number === "3"; i++){
    //     if(number =)
    //     return number;
    // }
  }
}
